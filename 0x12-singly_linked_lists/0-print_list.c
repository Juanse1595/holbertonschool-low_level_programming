/**
  * print_list - Prints all elements in list
  * @h: head pointer
  * Return: number of nodes
  */

size_t print_list(const list_t *h)
{
	size_t nodecount = 0;
	char *s = "(nil)";

	while (h != NULL)
	{

	}
	return (0);
}
